name: Continuous Persistent VPS

on:
  schedule:
    - cron: '0 */6 * * *'  # Every 6 hours
  workflow_dispatch:

jobs:
  vps-session:
    runs-on: ubuntu-latest
    timeout-minutes: 350  # Just under 6 hours

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set hostname to unixnodes
        run: sudo hostnamectl set-hostname unixnodes

      - name: Download VPS backup (if any)
        uses: actions/download-artifact@v4
        with:
          name: vps-backup
          path: ./backup
        continue-on-error: true

      - name: Install prerequisites
        run: |
          sudo apt update
          sudo apt install -y tmate curl unzip sudo net-tools neofetch openssh-server rsync
          sudo systemctl enable ssh
          sudo systemctl start ssh

      - name: Enable root login with password
        run: |
          echo "root:root" | sudo chpasswd
          sudo sed -i 's/^#\?PermitRootLogin.*/PermitRootLogin yes/' /etc/ssh/sshd_config
          sudo sed -i 's/^#\?PasswordAuthentication.*/PasswordAuthentication yes/' /etc/ssh/sshd_config
          sudo systemctl restart ssh

      - name: Restore persistent data
        run: |
          if [ -f ./backup/backup.zip ]; then
            unzip -o ./backup/backup.zip -d /
            echo "✅ Persistence restored"
          else
            echo "ℹ️ No backup found, starting fresh"
          fi

      - name: Start tmate session for SSH access
        uses: mxschmitt/action-tmate@v3
        with:
          limit-access-to-actor: true  # Only repo owner can access

      - name: Show SSH connection info
        run: |
          echo "=============================="
          echo "🔑 SSH (via GitHub runner public IP):"
          echo "ssh root@$(curl -s ifconfig.me)"
          echo "Password: root"
          echo "=============================="

      - name: Sleep to keep VPS alive
        run: sleep 21600  # 6 hours

      - name: Backup persistent data
        run: |
          sudo mkdir -p /opt/vps-backup
          # Save /home and /etc (you can expand this list)
          sudo rsync -a /home /etc /opt/vps-backup/
          sudo chown -R $USER:$USER /opt/vps-backup
          zip -r backup.zip /opt/vps-backup

      - name: Upload VPS backup artifact
        uses: actions/upload-artifact@v4
        with:
          name: vps-backup
          path: backup.zip
